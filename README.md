$ npm init -y

$ npm install express

$ npm install -g nodemon
and Run 
$ nodemon server.js

To add pacagek.json
"dev": "nodemon server.js" 
and Run 
$ npm run dev

$ npm install dotenv

$ npm install mongoose

